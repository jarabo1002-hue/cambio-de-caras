version: '3.8'

services:
  face-swap-app:
    build: .
    ports:
      - "3000:3000"
    volumes:
      - ./uploads:/app/uploads
      - ./results:/app/results
    environment:
      - PORT=3000
      - NODE_ENV=production
    restart: always
